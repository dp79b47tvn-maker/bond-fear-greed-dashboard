# 債券市場恐懼貪婪儀表板 ＋ 因子驗證分析報告
## 資料來源與計算邏輯完整說明（白話文版）

這份文件涵蓋兩個東西：

- **一、`chart/dashboard.html`** —— 每天看的即時儀表板
- **二、`factor_validation_report.html`** —— 一次性、可重跑的因子驗證分析報告

目的是把「資料到底從哪裡來、可不可信」、「每個數字到底是怎麼算出來的」，用完全不需要統計或程式背景也看得懂的方式講一遍。

---

## 第一部分：資料來源總覽與可信度

儀表板跟驗證報告底層用的原始資料完全相同，來自四個地方：

### 1. Yahoo Finance（用 yfinance 這個Python套件抓）

**抓的東西：**

| 代號 | 內容 |
|---|---|
| `ZN=F` | 10年期美國公債期貨收盤價 |
| `NQ=F` | 那斯達克100期貨收盤價 |
| `TLT` | 20年期以上美國公債ETF收盤價 |
| `SHY` | 1-3年期美國公債ETF收盤價 |
| `^MOVE` | MOVE指數（債券市場的「恐慌指數」，衡量利率選擇權的隱含波動度） |

**可信度：**
這是「非官方」管道——Yahoo沒有跟你簽任何服務保證書，理論上哪天說改版、說關閉都有可能，過去也真的發生過抓取方式失效需要更新程式的狀況。但實務上，Yahoo Finance的每日收盤價數據品質一直很穩定，可以拿去跟CME官方網站的收盤價逐日核對，數字一致。它不是「完全不可信」，而是「沒有正式合約保證」，長期用下去要有心理準備某天可能需要換一個資料來源。

**已確認回溯深度（2026-07實測）：**

| 代號 | 最早資料 |
|---|---|
| `ZN=F` | 2000-09 |
| `NQ=F` | 2000-09 |
| `TLT` | 2002-07 |
| `SHY` | 2002-07 |
| `^MOVE` | 2002-11 |

這五個都比原本擔心的「不夠深」還深很多，20年的分析需求都撐得住（只有TLT/SHY/MOVE剛好在「20年+5年暖身期」這個更嚴格的門檻下差一點，詳見第三部分因子驗證報告的說明）。

### 2. Treasury.gov（美國財政部官方網站）

**抓的東西：** 10年期公債殖利率、2年期公債殖利率

**可信度：** 這是美國聯邦政府官方公布的資料，是所有來源裡可信度最高的一個——這就是「真正的官方數字」，沒有任何轉手或猜測的成分。

### 3. FRED（美國聖路易斯聯邦準備銀行的公開資料庫）

**抓的東西：**

| 代號 | 內容 |
|---|---|
| `CPIAUCSL` | 消費者物價指數（CPI），用來算年增率 |
| `T10YIE` | 10年期損益兩平通膨率（債券市場自己對未來通膨的定價） |

**可信度：** FRED是聯準會體系底下的官方資料庫，這兩個數列都是美國政府/央行自己發布的官方統計，可信度同樣很高。CPI資料回溯到1947年，損益兩平通膨率回溯到2003年，都遠超過我們任何分析需要的長度。

### 4. 使用者自己輸入（Excel檔 `data_input.xlsx`）

**抓的東西：** `put_call_ratio`（選擇權賣權/買權比率）

**可信度：** 這欄目前**完全沒有資料**——這不是抓取失敗，是打從一開始就沒有任何免費的管道能拿到夠深的歷史Put/Call資料（有調查過付費/免費的選項，結論是免費來源的歷史深度都不夠回溯到2020年）。這個因子在儀表板跟驗證報告裡全程顯示「無資料」，不會用假數字填充，也不會靜悄悄地跳過不講。

---

## 第二部分：儀表板（dashboard.html）的計算邏輯

**核心概念：** 先把每個原始數字轉換成「這個數字在過去5年裡算高還是算低」的百分位分數（0到100分），這樣單位完全不同的東西（殖利率的%、期貨的點數、波動度指數）才能放在同一把尺上比較、平均。

### 滾動百分位評分法（幾乎每個因子都用這個）

白話講：拿「今天的數值」跟「過去5年（1825天）裡每一天的數值」比較，看今天贏過其中百分之多少天。比如算出來是98分，代表今天的數值比過去5年裡98%的交易日都高。

**好處：** 不管原始單位是殖利率(%)、期貨點數、還是波動度指數，換算成百分位之後全部都是0到100分，可以直接加總平均。

**重要規則（禁止引用未來資料）：** 這個5年視窗只會往「過去」看，絕對不會用到「未來」才發生的價格去算今天的分數。程式裡有一個自動檢查機制（`assert_no_lookahead_bias`），每次執行都會抽樣幾個歷史日期，假裝「只知道那天以前的資料」重新算一次分數，去跟正式輸出比對——只要有一天對不上，代表某個地方偷看了未來資料，程式會直接報錯，不會讓有問題的分數流出去。

### 七項因子，各自怎麼算

**【1】動能 —— ZN期貨對125日均線的乖離率**
怎麼算：`(ZN期貨現價 − ZN期貨過去125天的平均價) ÷ 平均價 × 100`，再把這個乖離率放進過去5年分佈裡取百分位。
白話：如果現在的價格持續站在半年均線之上，代表債市漲勢有慣性、資金持續湧入（貪婪）；如果深深跌破均線，代表拋售動能強（恐懼）。

**【2】強度 —— NQ期貨在過去52週高低區間的位置**
怎麼算：`(NQ期貨現價 − 過去252天最低價) ÷ (過去252天最高價 − 最低價) × 100`。這個因子比較特別，不走5年百分位那一套，是直接算「現在的價格站在過去一年最低到最高之間的第幾%位置」。
白話：越接近一年最高點，代表市場（用科技股期貨代表）風險偏好越高（貪婪）；越接近一年最低點，代表越悲觀（恐懼）。這個因子反映的是「股市情緒」，跟其他幾項反映「債市情緒」的角度不同，放在一起可以看出股債情緒有沒有背離。
*已知限制：因為那斯達克100長期處於上升趨勢，這個分數大部分時間都貼在接近滿分附近，恐懼、中性區間的樣本天數比較少。*

**【3】存續期間避險 —— TLT對SHY的40日報酬差**
怎麼算：TLT過去40天的報酬率減掉SHY過去40天的報酬率，再把這個利差放進5年百分位。TLT是長天期公債ETF（對利率變化敏感），SHY是短天期公債ETF（對利率變化不敏感）。
白話：如果長天期公債明顯跑贏短天期，代表市場願意冒著「利率變化風險」去追價買長天期債券（貪婪、積極避險）；如果短天期跑贏，代表市場只想要安全、不想冒風險（恐懼）。

**【4】Put/Call —— 目前無資料**
怎麼算（有資料時）：把Put/Call比率取5日平均，放進5年百分位，再用100減掉（因為比率越高代表越多人買賣權避險，等於越恐懼，分數要反過來）。
*目前狀態：完全沒有歷史資料，儀表板跟報告裡都誠實顯示「無資料」。*

**【5】波動度 —— MOVE指數90日滾動偏態係數**
怎麼算：MOVE指數過去90天數值的「偏態係數」（skewness，衡量分佈是不是往一邊拉長尾巴），放進5年百分位，再用100減掉做反轉。
白話：偏態係數越高，代表最近MOVE指數常常突然飆高、有明顯的右尾（市場情緒不穩定、恐慌情緒容易被放大），所以分數要偏低（恐懼）。
*這個算法本身在專案裡改過一版——原本是看「MOVE現值離50日中位數偏離多少%」，後來發現偏態係數更能抓到「波動度正在轉折」這種細膩變化，才改成現在這個版本。舊算法變成旁邊的補充參考欄位，不參與分數計算。*

**【6】殖利率曲線形狀 —— 10年期減2年期利差（2s10s）**
怎麼算：`10年期殖利率 − 2年期殖利率`，直接放進5年百分位（不用反轉）。
白話：利差越高（曲線越陡）代表經濟循環相對正常、市場不擔心衰退（貪婪）；利差越負（倒掛越深）代表市場擔心經濟衰退（恐懼）。
*已知限制：殖利率曲線倒掛到真正衰退發生，中間會等多久並不固定，這個因子只能當方向性參考，不能拿來精準抓時間點。*

**【7】通膨意外 —— CPI年增率減10年期損益兩平通膨率**
怎麼算：CPI年增率（實際發生的通膨）減掉10年期損益兩平通膨率（債券市場自己對未來通膨的定價），放進5年百分位，再用100減掉做反轉。
白話：如果實際通膨遠高於市場原本的預期定價，代表通膨「意外地」比大家想的嚴重，是恐慌訊號（分數應偏低）。
*已知限制：原本想用「CPI/PCE實際值 vs 經濟學家共識預期」，但調查後發現免費資料來源都拿不到回溯夠深的「共識預期」歷史資料，改用「債券市場自己的定價」當替代基準——概念上比較接近「市場對通膨的預期是否落後於實際數據」，跟真正的「單次公布意外/失望」不完全是同一件事，這點在儀表板的因子說明裡也有註明。*

### 綜合分數

怎麼算：七項分數（動能、強度、存續期間避險、Put/Call、波動度、殖利率曲線形狀、通膨意外）的**等權重平均**。某一項當天沒有資料時自動排除、只用其餘有資料的項目平均，不會拿假數字去湊。

**分級標籤**（跟CNN原版一致）：

| 分數區間 | 標籤 |
|---|---|
| 0-24 | 極度恐懼 |
| 25-44 | 恐懼 |
| 45-55 | 中性 |
| 56-75 | 貪婪 |
| 76-100 | 極度貪婪 |

**顏色邏輯：** 這個專案用的是台灣市場慣例——紅色＝貪婪、綠色＝恐懼，跟美式CNN原版的紅綠邏輯是相反的，這是刻意的在地化選擇。

### 情境加權分數（實驗性功能）

這是儀表板上額外可以打開的一條虛線，**不是取代**上面的等權重版本。

**概念：** 不同的Fed利率循環階段（升息／暫停／降息），各個因子跟未來報酬的實際相關性不一樣，所以讓權重跟著目前所處的階段動態調整，理論上應該比固定等權重更準。

**權重是怎麼決定的：** 拿每個Fed階段下、各因子分數對未來40天ZN期貨報酬的Spearman相關係數，算出來的權重「凍結」在某個時間點（2026-07-17），之後不會每天重新校準——這樣做的原因是：如果每天都重新算一次權重，就永遠沒有「還沒被權重看過的新資料」可以拿來驗證這套方法到底準不準。

> **重要老實話：** 這個功能目前的「表現看起來比較準」主要是因為權重本身就是拿歷史資料反推出來的，再用同一份資料去驗證，本來就會比較好看，這不是獨立的證據。真正有意義的驗證，要等凍結日期之後累積的新資料夠多，才算數（有一支獨立的 `validate_regime_weights.py` 腳本專門做這件事）。

---

## 第三部分：因子驗證分析報告（factor_validation_report.html）的計算邏輯

這是一份獨立的、一次性但可重複執行的分析（`python3 factor_validation_analysis.py`），目的是給你拿去跟主管討論「哪些因子該留、哪些該重新設計」。底層抓的原始資料來源跟第一部分完全一樣（Yahoo/Treasury/FRED），只是會額外多抓更久以前的歷史資料，讓分析能拉長到10年、20年。

### 一、資訊係數（IC）分析

怎麼算：用**Spearman等級相關係數**，看「今天的分數排名」跟「未來N天（5/10/20/60天）ZN期貨報酬率的排名」有沒有關係。

**跟一般相關係數不一樣的地方：** 一般人熟悉的皮爾森相關係數看的是「數值」是不是呈直線關係；Spearman看的是「排名」，比較適合我們這種0-100分的排序型分數，不要求兩者是直線關係。

**避免統計灌水的關鍵設計——非重疊取樣：** 如果每天都拿「今天分數」配對「未來20天報酬」，那「今天」跟「明天」這兩筆樣本，未來20天裡有19天是重疊的，等於同一件事被算了兩次，會讓樣本數看起來很多、但其實「真正獨立」的樣本數少很多，統計上的可信度會被高估（灌水）。這份報告全部改成「每隔N天（N=驗證的報酬天數）才取一次樣本」，樣本數天生比較少，但每個樣本點之間不會互相重疊污染。

**正負號怎麼解讀：** 使用者的操作假設是「恐懼買入」（分數低、未來報酬應該高，所以IC應該是負的）。這份報告如實呈現算出來的正負號，不會為了迎合這個假設而扭曲呈現方式——這正是這份報告存在的目的。實測結果是：七個因子加綜合分數，**幾乎全部呈現正號**（動能延續型態，分數越高、未來報酬歷史上也越高），只有殖利率曲線形狀接近零、方向不確定。

### 二、分位數分桶分析（5組 / 10組 / 20組，三個版本並存）

怎麼算：用pandas的 `qcut`，把每個因子的歷史分數依照數值高低切成N等分（每組筆數盡量平均），看每一組「未來20天平均報酬率」是多少，畫成長條圖。

**三個版本的差異：**

| 版本 | 分組方式 | 時間範圍 | 取樣方式 |
|---|---|---|---|
| 5組 | CNN原版固定五級（極度恐懼～極度貪婪） | 現有資料（約5.5年） | 全樣本，不做非重疊取樣 |
| 10組 | qcut自動均分 | 目標10年 | 非重疊取樣 |
| 20組 | qcut自動均分 | 目標20年 | 非重疊取樣 |

三個版本互相對照用，不是取代關係，可以看切法變細之後，報酬隨分數遞增/遞減的形狀是不是依然一致。

**單調性：** 一個介於-1到+1之間的數字，衡量「報酬有沒有隨著分數規律變化」。越接近-1代表報酬隨恐懼→貪婪單調遞減（符合「恐懼買入」假設）；越接近+1代表單調遞增（動能延續，跟假設相反）。

**樣本數不足的警示：** 分組切得越細，每組樣本數自然越少，估計出來的平均值就越不穩定。報告裡每一組都明確標示樣本數n，如果低於10筆，圖表上那根長條會加上斜線網底、紅色邊框，數字旁邊加星號，提醒這組數字的可信度比較低，不該跟樣本充足的組別一視同仁地解讀。

**「延伸歷史資料」是怎麼多抓出來的：** 5年百分位需要「暖身期」——如果想要分數本身有滿10年可用，原始價格資料要往前多抓5年（給百分位視窗填滿），所以10年分析實際上會去抓大約15年的原始資料；20年分析會去抓大約25年的原始資料。這些原始資料的實際回溯深度（尤其是MOVE指數，這是使用者特別關心會不會不夠深的因子）在跑分析之前都會先實際查證、印出來，不會用假設帶過——查證結果是MOVE指數等來源其實回溯得比想像中更深（2002年就有資料了），10年的分析完全沒問題，只有在20年這個更嚴格的門檻下，MOVE、TLT/SHY、損益兩平通膨率這三個來源才會剛好不夠深，報告裡每個因子都誠實列出自己實際用到的區間，不會假裝全部都湊滿20年。

### 三、Leave-one-out（移除測試）

怎麼算：把七項因子輪流從綜合分數的計算裡拿掉一項，只用剩下六項重新算一次綜合分數，對這個「少一項」的綜合分數重新做一次IC分析（用20天窗口），看IC有沒有變化。

白話：如果拿掉某個因子後，IC幾乎沒變，代表這個因子對整體預測力的邊際貢獻很小（可能跟其他因子講的是類似的事）；如果拿掉後IC明顯變差，代表這個因子提供了獨特、不可取代的資訊。

### 四、因子相關係數矩陣

怎麼算：把七項因子的歷史分數放在同一張表，用pandas的 `.corr()`（皮爾森相關係數）算出兩兩之間的相關性，用seaborn畫成熱力圖。

白話：如果兩個因子的相關係數很高（比如動能跟存續期間避險算出來0.68），代表這兩個因子歷史上經常同步變動，可能在講重複的事，值得考慮要不要精簡。

### 五、策略回測（quantstats）

**部位規則：** `部位大小 = (50 − 當日分數) ÷ 50`。分數等於50時空手，分數是0（極度恐懼）時滿額做多，分數是100（極度貪婪）時滿額做空，中間平滑過渡。48到52分之間設一個「死區」，強制空手，避免分數在中性附近小幅雜訊震盪時頻繁進出。

**避免引用未來資料：** 部位是用「昨天收盤時的分數」決定的，套用在「今天」的報酬上（也就是position往後移一天才生效），不會用今天才知道的分數去交易今天的報酬。

**用quantstats算出來的績效指標：** 夏普比率（風險調整後報酬）、索提諾比率、最大回撤、年化報酬、勝率（quantstats叫Win Days）、年化波動度，並跟「買進持有ZN不動」的基準策略比較。

這個回測對綜合分數、以及七個因子各自單獨當訊號來源，都各跑一次，方便比較單一因子表現跟綜合分數表現的差異。綜合分數還有一份完整的quantstats原生報告（含14張圖表），直接內嵌在HTML報告裡的一個iframe裡面，開啟就能看，不需要另外開檔案。

> **實際發現過的一個真實bug：** quantstats的 `metrics()` 函式回傳的表格欄位是 `['Benchmark', 'Strategy']`，早期版本的程式碼用位置索引（`.iloc[0]`）去抓數字，結果抓到的是Benchmark（基準）欄位、不是Strategy（策略）欄位——導致每個因子的回測數字全部顯示成一模一樣的買進持有基準表現。這個bug已經修正（改成用欄位名稱 `'Strategy'` 明確抓取），修正前後的報告版本可以在git歷史裡對照。

### 六、樣本穩定性檢查（前半段 vs 後半段）

怎麼算：把全部歷史資料從中間切成兩半（前半段、後半段，各約1196天），上面第一到五項的分析，除了跑一次全樣本，也分別在前半段、後半段各跑一次。

白話：如果同一個因子在前後半段的IC正負號完全相反，代表這個訊號本身不穩定，可能是被某一段特別的時期（例如2020年疫情、2022年升息）主導，不該直接當成放諸四海皆準的規則來用。

---

## 第四部分：整體可信度總結

**資料來源本身：** Treasury.gov跟FRED是官方資料，可信度最高；Yahoo Finance是非官方但長期穩定、可跟CME官方數字核對的來源；Put/Call完全沒有資料，兩份文件都誠實顯示「無資料」，不會用假數字填充。

**計算邏輯本身：** 所有滾動計算都經過自動化的「禁止引用未來資料」檢查，每次重跑都會驗證一次。因子驗證報告額外做了非重疊取樣、樣本數不足警示、前後半穩定性比較這幾層，目的都是避免統計數字「看起來」比實際上更可靠。

> **最重要的一句話：** 這整套框架目前呈現出來的訊號，大部分都是弱訊號（IC普遍在0.1上下），方向也跟「恐懼買入」的原始假設相反（動能延續型態居多）——這不是程式有錯，是歷史資料如實反映出來的結果。這份文件的目的是讓你清楚知道每個數字是怎麼來的、可信度打幾折，而不是說服你這套系統已經是一個可靠的交易訊號。
