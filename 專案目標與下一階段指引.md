# 專案目標釐清與下一階段工作指引

> 日期：2026-08-04
> 依據：與 mentor 的專案討論結論 + 現有資料實測結果

---

## Part 1｜專案目標到底是什麼（跟 mentor 討論的釐清）

### 1.1 兩種形狀：動能延續 vs 反轉

分桶圖的橫軸是「當天的恐懼貪婪分數」（左＝恐懼、右＝貪婪），縱軸是「之後 N 天的實際報酬」。兩種完全相反的形狀：

| 形狀 | 長相 | 意義 | 交易意涵 |
|---|---|---|---|
| **動能延續型** | 左低右高（往上） | 恐懼之後繼續跌、貪婪之後繼續漲 | 順勢：追趨勢 |
| **反轉型** | 左高右低（往下） | 恐懼之後會漲、貪婪之後會跌 | 逆勢：別人恐懼我貪婪 |

CNN 恐懼貪婪指數之所以出名，用的是**反轉型**的邏輯。這是這類情緒指標的核心價值主張。

### 1.2 mentor 的重點：不是推翻整體形狀，而是只看「兩端」

這是最容易誤解的地方。mentor **不是**說「你們測錯了，應該要是反轉型」。他的意思是：

> **整體斜率長怎樣其實不太重要，因為中間那幾桶就是普通盤整、本來就沒什麼訊息量。一個情緒指標真正有價值的地方只在極端——當市場真的在恐慌、或真的在狂歡的時候。**

所以要找的形狀是：**整體可能是往上的（動能），但最左邊一小撮往上翹（恐懼→正報酬）、最右邊一小撮往下掉（貪婪→負報酬）**。

**關鍵陷阱**：這種「中間往上、兩端勾回」的形狀在統計上**不是單調的**，所以現在用的「單調性（monotonicity）」指標反而會**扣分**。光看那個數字會直接漏掉 mentor 要找的東西——這正是為什麼需要改分析方式。

### 1.3 為什麼「即便樣本數不多」還是要看

因為**極端本來就稀少，這是定義問題不是資料問題**。2408 個交易日裡，綜合分數低於 10 分的只有 28 天，而且是重疊取樣（可能其實只是 1-2 個獨立事件，例如某一波急跌）。

統計上不可能宣稱顯著。但邏輯是：

- 這是**「找線索」不是「下結論」**——探索性分析，不是拿來當統計證據
- 如果連在最極端、理論上訊號最強的地方都完全看不到反轉的影子，那這個指標當逆勢工具就是沒救了
- 反過來說，只要極端處有苗頭，就值得繼續往下挖（換標的、換天期、加條件）

### 1.4 目前實際數據長什麼樣（2026-08-04 實測）

標的：ZN 期貨；期間：2020-01-01 ~ 2026-08-04（2408 天）
綜合分數分布：中位數 52.9、10% 分位 27.8、90% 分位 71.1

**一定要跟基準線比**——這段期間債券整體是跌的，不減掉基準線會誤以為「哪裡都沒訊號」。

| 區間 | 未來5日超額 | 未來20日超額 | 未來60日超額 | 樣本天數 |
|---|---|---|---|---|
| 恐懼端 0-10 | −0.32% | **−1.21%**（勝率4%） | **+0.64%**（勝率71%） | 28 |
| 恐懼端 0-20 | −0.29% | −0.84% | −0.95% | 148 |
| 恐懼端 0-25 | −0.12% | −0.42% | −0.75% | 204 |
| 貪婪端 75-100 | +0.07% | +0.26% | +0.56% | 104 |

**目前的解讀（誠實版）**：

1. **整體看起來是動能延續，不是反轉**——恐懼端短中期繼續跌、貪婪端繼續漲。跟 CNN 股市版的經典用法相反。
2. **唯一的反轉苗頭在「極度恐懼 × 長天期」**：0-10 分區間在 60 日翻正（+0.64%、勝率71%）。也就是說「極度恐懼之後要等比較久（約3個月）才會反轉」，短中期反而繼續受傷。
3. **但這個苗頭極度不可靠**：只有 28 天樣本、重疊取樣、可能只是 1-2 個獨立事件。**不能當結論，只能當待驗證的假設。**
4. 貪婪端完全沒有反轉跡象（三個天期都是正超額）。

---

## Part 2｜接下來要做的事

### A. 所有分析圖都要加上「完整統計分布圖」

**現在的問題**：目前分桶分析只輸出「每桶的平均報酬」一個數字。平均值會被極端值拉走，而且看不出離散程度——兩個平均值一樣的桶，可能一個是穩定小賺、一個是大賺大賠互相抵銷，這在決策上完全不同。

**要補的圖**（每個因子、每個天期都要）：

1. **箱型圖（box plot）**：每個分桶一個箱子，看中位數、四分位距、離群值。這是最直接能看出「分布」而不只是「平均」的圖。
2. **小提琴圖（violin plot）或分布密度圖**：看每個桶的報酬分布形狀（單峰？雙峰？偏態？）。雙峰特別重要——代表這個桶其實混了兩種不同狀態。
3. **散佈圖（scatter）＋回歸線**：橫軸分數、縱軸未來報酬，每一天一個點。可以直接看出關係是線性、非線性、還是根本無關。
4. **樣本數標註**：每個桶旁邊一定要標 n，尤其極端桶。
5. **勝率長條圖**：每個桶「報酬 > 0 的比例」，跟平均報酬並列。平均可能被單一極端值主導，勝率不會。

**實作位置**：`factor_validation_analysis.py` 已有 `bucket_chart_base64()`、`decile_chart_base64()`、`fig_to_base64()` 等既有函式可以參考/擴充；`factor_screening.py` 的 `render_screening_sections()` 是每個因子報告的組裝點。

**重點提醒**：分桶方式要改。現在用 `pd.qcut` 等分桶（每桶樣本數一樣多），會把極端稀釋掉。應該**同時提供**：
- 等分桶（看整體趨勢）
- **固定門檻分桶**（用專案既有的 0-24 / 25-44 / 45-55 / 56-75 / 76-100 五級，或直接指定前5%/後5%）—— 這才看得到極端

---

### B. 12 個因子的排列組合回測

#### B.1 目前有哪 12 個因子

**官方七因子**（已在儀表板，`factor_definitions.json`）：

| # | key | 名稱 | 模式 |
|---|---|---|---|
| 1 | momentum | 動能 — US 10Y 125日均線價差 | ma_spread |
| 2 | strength | 強度 — NQ期貨52週高低區間位置 | range_position |
| 3 | duration | 存續期間避險 — TLT vs SHY 40日報酬差 | return_spread |
| 4 | putcall | Put/Call 選擇權比率 | moving_average |
| 5 | move | 波動度 — MOVE指數90日偏態係數 | rolling_stat |
| 6 | curve | 殖利率曲線形狀 — 10Y減2Y利差 | value_spread |
| 7 | inflation | 通膨意外 — CPI年增率減損益兩平通膨率 | value_spread |

**候選五因子**（已在篩選平台測過，`factor_screening_registry.json`）：

| # | key | 名稱 | 模式 | 單獨IC |
|---|---|---|---|---|
| 8 | cand_copper_gold | 銅金比 40日報酬差 (HG=F vs GC=F) | return_spread | **+0.124** |
| 9 | cand_credit_spread | 投資級企業債信用利差 (BAA10Y) | ma_spread | +0.025 |
| 10 | cand_swap_spread | 10年期美債 Swap Spread | value_spread | −0.043 |
| 11 | cand_sofr_ted_spread | SOFR與3M美債利差 | value_spread | +0.063 |
| 12 | cand_inflation_1y | 1年期通膨預期 125日均線價差 | ma_spread | −0.092 |

> ⚠️ **putcall 目前完全沒有資料**（`data_input.xlsx` 沒填），實際可用的只有 11 個。回測時要嘛排除它、要嘛先補資料。

#### B.2 資料準備（這步不能跳）

**候選五因子的分數序列目前不在 `bond_fear_greed_v2.csv` 裡**，只有官方七因子 + `promoted_us10y_score`。所以第一步要：

1. 用 `transform_modes.build_candidate_score(config, df, fetch_yahoo, fetch_fred)` 把 5 個候選因子的每日分數算出來
2. 合併成一張「12 因子 × 每日分數」的寬表，存成中繼檔（例如 `factor_scores_matrix.csv`）
3. 之後所有回測都吃這張表，不要每次重抓（重抓一次 20 年資料要好幾分鐘）

#### B.3 組合數量與方法

11 個有效因子的所有非空子集 = 2^11 − 1 = **2047 種組合**。計算上完全可行（每種組合就是等權平均後算一次 IC/回測）。

**建議的評估流程**：

```
對每一種組合 C：
  1. 綜合分數 = C 裡各因子分數的等權平均（skipna）
  2. 計算：
     - IC（Spearman，多天期：5/20/60日）
     - 分桶單調性
     - ★ 兩端反轉指標（見下方 B.4）
     - 回測績效（夏普、最大回撤、勝率）
  3. 記錄結果
```

#### B.4 ★ 評估指標一定要包含「兩端反轉」

這是這次的核心。**不能只用 IC 和單調性排名**——那兩個指標都會獎勵「動能延續型」、懲罰 mentor 要找的形狀。

建議新增一個自訂指標，例如：

```
尾端反轉分數 = (貪婪端超額報酬 × −1) + (恐懼端超額報酬)
```

- 恐懼端超額為正（恐懼後會漲）→ 加分
- 貪婪端超額為負（貪婪後會跌）→ 加分
- 兩者都符合 → 分數最高

並且**同時報告該組合在兩端各自的樣本數**，樣本太少的組合要標警示。

#### B.5 ⚠️ 過度配適（overfitting）的嚴重風險

**這是整個 B 部分最大的陷阱，一定要處理：**

跑 2047 種組合然後挑「最好的那個」，幾乎保證會挑到一個**只是運氣好**的組合。這就是典型的多重比較偏誤——測 2047 次，就算全部都是隨機雜訊，也會有幾十個看起來「顯著」。

**必須做的防護**：

1. **樣本外驗證（walk-forward）**：用 2020-2024 選出最佳組合，拿 2025-2026 驗證。如果樣本外表現直接崩掉，代表是配適出來的。
2. **報告「前N名的表現分布」而不只是第一名**：如果第1名跟第50名差不多，代表排名沒有意義。
3. **看穩定性**：同一個組合在不同天期（5/20/60日）、不同 Fed 循環階段是否都表現一致？只在單一天期出色的通常是雜訊。
4. **簡單優先**：如果 3 因子組合跟 9 因子組合表現差不多，選 3 因子的（少一個參數少一分過度配適風險）。

#### B.6 刪除因子的判斷準則

建議依序檢查，符合任一條就是刪除候選：

| 準則 | 說明 |
|---|---|
| 沒有資料 | putcall 目前完全空白 |
| 單獨 IC 接近 0 且方向不穩 | 例如 `cand_credit_spread` IC 僅 +0.025 |
| 跟既有因子相關係數過高 | 訊息重複、加進來沒有增量價值（篩選平台第四關已有此檢定，門檻 0.6） |
| 拿掉它綜合分數反而變好 | Leave-one-out ΔIC 為正 |
| 在樣本外驗證中表現反轉 | 樣本內好、樣本外壞 = 配適 |

現有工具：篩選平台的**第四關（增量價值）**已經在做「相關係數 + LOO ΔIC」，可以直接沿用邏輯，只是要從「一次測一個候選」擴充成「批次測所有組合」。

---

## Part 3｜踩過的坑（重要，避免重犯）

### 3.1 不要用「讀 CSV 再寫回同一個 CSV」的方式測試

**2026-08-04 踩到的坑**：本機測試腳本 `test_dashboard_full.py` 會讀 `bond_fear_greed_v2.csv` 當假資料源，然後執行 `update_dashboard.py` 又把結果寫回同一個 CSV。

問題是：**CSV 只存 2020 年以後的資料，沒有 5 年百分位需要的暖身期**。每跑一次，滾動百分位就少算一段，有效資料範圍就往後縮一次。連續跑幾次之後：

- `momentum_score` 有效範圍剩 2025-05 之後（原本應該 2020-01）
- `inflation_score` 只剩 2025-12 之後
- 但 `composite_score` 因為是 skipna 平均，**看起來還是滿的**（實際上大部分日期只剩 strength 一個因子在撐）

結果就是分布嚴重扭曲（中位數變成 75.6、超過 10% 的日期是滿分 100），拿去做的所有分析全是錯的。

**教訓**：
- 測試要用真的 `update_dashboard.py`（它會重抓含暖身期的完整資料），或準備一份獨立的假資料檔，**絕對不要讓輸出覆蓋輸入**
- 這個 CSV 在 `.gitignore` 裡（正式站由 CI 每天重新產生），所以只影響本機、沒污染到正式站
- **發現異常的訊號**：如果各因子的 `notna()` 天數差很多、或分布明顯偏斜，先檢查資料再檢查邏輯

### 3.2 一定要跟基準線比

2020-2026 債券整體是跌的（未來20日平均報酬 −0.136%）。不減掉這個基準線，所有分桶看起來都是負的，會誤判成「沒有訊號」。

### 3.3 重疊取樣要標註

用「未來20日報酬」時，連續的每一天樣本高度重疊。148 天的樣本可能只代表 7-8 個獨立事件。報告一定要同時標**原始天數**和**大致的獨立事件數**，不要讓讀者以為統計力比實際強。

---

## 附錄：立即可執行的第一步

建議照這個順序：

1. **先修好 putcall 資料**（或正式決定放棄它）——12 個因子有 1 個是空的，會影響所有組合計算
2. **建立 12 因子分數矩陣中繼檔**（B.2）——這是後面所有工作的基礎
3. **先做單因子的分布圖**（A）——在跑組合之前，先確認每個因子單獨的分布長怎樣、極端處有沒有苗頭
4. **再跑組合回測**（B）——並且從一開始就把 walk-forward 驗證納入流程，不要等做完才補
