# 完整計算流程逐步講解：從原始數據到分數與驗證

> 用 **2026-08-13 這一天的真實資料**，把從最底層原始數據一路算到 IC 驗證數字的每一步完整走一遍。
> 所有數字都是從 `bond_fear_greed_v2.csv` 實際撈出來、可重現的。
>
> *最後更新：2026-08-13（因子從七項改為兩項後全面重算）*

---

## 全景圖：資料是怎麼一路流動的

```
原始市場數據  →  中間計算欄位  →  0~100分數  →  綜合分數  →  IC驗證
(Yahoo/Treasury)  (價差/報酬差)   (5年百分位)   (兩項平均)   (跟未來報酬比對)
```

每一格都是下一格的原料。以下用 2026-08-13 這天貫穿全部。

---

## 第一站：原始數據長什麼樣

這天抓進來的原始數字：

| 欄位 | 數值 | 來源 | 用途 |
|---|---|---|---|
| `UST_10Yr` | 4.68 % | Treasury.gov | 動能因子；也是驗證標的 |
| `HG_futures` | 6.587 | Yahoo Finance (HG=F) | 銅金比 |
| `GC_futures` | 4491.80 | Yahoo Finance (GC=F) | 銅金比 |

注意 HG=F 是美元/磅、GC=F 是美元/盎司，**單位不同**。這不影響計算，因為銅金比因子用的是
「兩者各自的報酬率相減」，報酬率本身是無單位的百分比。

---

## 第二站：核心機制——滾動百分位（整個系統的心臟）

每個因子的原始數值單位都不一樣（%、點數、比值），沒辦法直接平均。
解法是把每個數值換算成「**這個數字在過去 5 年裡算高還是算低**」的百分位分數。

```
分數 = (過去1825天裡「數值 ≤ 今天」的天數) ÷ 1825 × 100
```

算出來 98 分，代表今天的數值比過去 5 年裡 98% 的交易日都高。

**禁止引用未來資料**：這個 5 年視窗只往「過去」看。程式裡有自動檢查
（`assert_no_lookahead_bias`），每次執行都會抽樣幾個歷史日期，假裝「只知道那天以前的資料」
重算一次分數去跟正式輸出比對——只要有一天對不上就直接報錯中止。

---

## 第三站：兩個因子，各自怎麼從原始數據變成分數

### 【1】動能 → 35.18 分

**步驟 1｜算 125 日均線**

```
UST10Y_SMA125 = US 10Y 過去125個交易日的平均 = 4.4994
```

**步驟 2｜算價差**

```
momentum_spread = 4.68 − 4.4994 = +0.1806
```

殖利率高於自己的均線 0.18 個百分點。

**步驟 3｜丟進 5 年百分位**

過去 1825 天裡，有 **1183 天**的價差 ≤ 今天的 +0.1806：

```
百分位 = 1183 ÷ 1825 × 100 = 64.82
```

**步驟 4｜反轉**

```
momentum_score = 100 − 64.82 = 35.18
```

**為什麼要反轉**：殖利率跟債券價格是反向的。殖利率高於均線代表價格跌破均線、拋售動能強，
那是**恐懼**，分數應該低。不反轉的話方向會顛倒。

### 【2】銅金比 → 31.29 分

**步驟 1｜各自算 60 日報酬率**

```
HG_ret = (6.587 / 60天前的銅價 − 1) × 100 = +2.4337 %
GC_ret = (4491.80 / 60天前的金價 − 1) × 100 = +6.5670 %
```

**步驟 2｜相減得報酬差**

```
copper_gold_spread = 2.4337 − 6.5670 = −4.1333
```

過去 60 天，銅比金弱了 4.13 個百分點。

**步驟 3｜丟進 5 年百分位（不反轉）**

過去 1825 天裡，有 **571 天**的報酬差 ≤ 今天的 −4.1333：

```
copper_gold_score = 571 ÷ 1825 × 100 = 31.29
```

**為什麼不反轉**：利差越高＝銅相對金越強＝景氣/風險偏好回升＝**貪婪**，分數本來就該高。
方向已經對了。

---

## 第四站：綜合分數 → 33.23 分「恐懼」

```
composite_score = (35.18 + 31.29) ÷ 2 = 33.23
```

兩項**等權重平均**。某項當天沒資料就自動排除、只用其餘項目平均，不會拿假數字去湊
（`skipna=True`）。

**分級標籤**：

| 分數區間 | 標籤 | |
|---|---|---|
| 0–24 | 極度恐懼 | |
| 25–44 | 恐懼 | ← **33.23 落在這裡** |
| 45–55 | 中性 | |
| 56–75 | 貪婪 | |
| 76–100 | 極度貪婪 | |

**顏色邏輯**：這個專案用台灣市場慣例——紅色＝貪婪、綠色＝恐懼，跟美式 CNN 原版相反，
這是刻意的在地化選擇。

---

## 第五站：這個分數到底準不準？——IC 驗證

分數算出來只是描述「現在的情緒」，接下來要問的是：**這個分數對未來有沒有預測力**。

用的方法是 **IC（Information Coefficient，資訊係數）**：把「每一天的分數」跟
「那天之後 N 天的實際報酬」做 Spearman 等級相關。

### 實際算一次

**驗證標的**：`UST_10Yr` 殖利率變動（單位 bp）。

> **為什麼是殖利率而不是 ZN 期貨價格**：期貨每季轉倉會製造人為的價格跳空，
> 那個跳空不是真實報酬，會汙染統計。改用殖利率就沒有這個問題。
> 詳見 `ZN期貨轉倉問題查證.md`。

**步驟 1｜算每一天的未來 20 日殖利率變動**

```
fwd_20d_bp = (20個交易日後的殖利率 − 今天的殖利率) × 100
```

**步驟 2｜跟分數做 Spearman 相關**

```
綜合分數 20日 IC（重疊取樣，n=2397）= +0.0205
```

### 這個 +0.0205 怎麼解讀（最重要的部分）

**正負號的意思**：分數高（貪婪）→ 之後殖利率**上升** → 債券價格**下跌**。
所以正的 IC 代表「**動能延續**」：貪婪之後續跌、恐懼之後續漲。

如果 IC 是負的，才代表「**反轉**」：貪婪之後價格反而漲、恐懼之後反而跌。

**數值大小的意思**：+0.02 是**非常弱**的相關。實務上一般認為 |IC| > 0.05 才有參考價值，
> 0.1 算不錯。目前這個綜合分數的預測力**接近沒有**。

**為什麼會這麼低**：兩個因子方向相反（相關係數 −0.39），平均之後互相抵銷。
單看銅金比自己的 20 年 IC 是 **+0.126**，動能是 **−0.046**——合成反而把訊號洗掉了。
這是已知問題，詳見 `HANDOVER.md` 第 8 節。

### 重疊 vs 非重疊取樣

同一份資料可以用兩種方式取樣：

- **重疊取樣**：每天都取一個樣本點。樣本數大（n≈2400），但相鄰樣本的報酬窗口高度重疊、
  彼此高度自相關，「有效」樣本數其實沒那麼多。
- **非重疊取樣**：每隔 20 天才取一個點（n≈120）。樣本間互相獨立，統計上比較乾淨，
  但樣本數少。

平台**兩個版本都算、並列呈現**，判斷以重疊版本為主、非重疊當對照。
**看到大 n 不要直接當成統計基礎穩固**——分桶統計裡「178 個交易日」換算成互不重疊的
獨立事件可能只剩 6 筆。

---

## 第六站：因子篩選平台的報告在做什麼

每個因子在 `chart/screening_*.html` 都有一份完整報告，除了上面的 IC 之外還有：

| 區塊 | 在回答什麼問題 |
|---|---|
| **Part A 統計分布** | 箱型圖／小提琴圖／勝率，特別檢視極度恐懼(0–24)與極度貪婪(76–100)兩端有沒有反轉跡象 |
| **第一關 資料健檢** | 資料夠不夠長、缺值多不多、有沒有偷看未來 |
| **第二關 單獨效力** | IC 夠不夠強、分桶單調不單調 |
| **第三關 穩定性** | 前半段跟後半段的 IC 同不同號、跨 Fed 循環穩不穩 |
| **第四關 增量價值** | 跟現有因子重不重複、加進來會不會讓綜合分數變好 |
| **第五關 可實作性** | 換手率多高（交易成本的代理指標） |
| **回測分析** | 照這個訊號實際下單會怎樣：權益曲線、Sharpe、最大回撤、樣本內外衰退 |

回測的部位規則是 `(50 − 分數) ÷ 50`，48–52 死區強制空手，
**t 日收盤的分數決定 t 日部位、賺 t 到 t+1 的報酬**，不使用未來資料。
必附「無條件買進持有」對照組——沒有基準的話很容易把「債券本來就在漲」誤認成「因子有效」。

---

## 結語：完整鏈條

```
Treasury.gov 4.68%  ─┐
                     ├→ 價差 +0.1806 → 百分位 64.82 → 反轉 → 動能 35.18 ─┐
125日均線 4.4994    ─┘                                                   │
                                                                          ├→ 綜合 33.23「恐懼」
HG=F 6.587  → 60日報酬 +2.43% ─┐                                         │
                                ├→ 報酬差 −4.13 → 百分位 → 銅金比 31.29 ─┘
GC=F 4491.80 → 60日報酬 +6.57% ┘
                                                    ↓
                                        跟未來20日殖利率變動做Spearman
                                                    ↓
                                              IC = +0.0205
                                        （動能延續方向，但強度接近零）
```

每一步都可以自己驗算：原始數據在 `bond_fear_greed_v2.csv`，
計算邏輯在 `update_dashboard.py` 的 `compute_scores()`（只有 30 行），
參數全部在 `factor_definitions.json`。
